pipeline{
    agent any
    stages{
        stage("A"){
            steps{
                script{
                    echo "GIT_COMMIT: ${GIT_COMMIT}"
                }
            }
        }
    }
}